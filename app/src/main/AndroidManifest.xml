<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.smartfieldservice">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/smart_field_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/smart_field_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:allowBackup">
        <activity android:name=".Review" />
        <activity android:name=".bKask" />
        <activity android:name=".PaymentCategory" />
        <activity android:name=".Jobs" />
        <activity android:name=".FeedBack" />
        <activity android:name=".Message" />
        <activity android:name=".ViewCustomerOrder" />
        <activity android:name=".Payment" />
        <activity android:name=".LoginForDeliveryMan" />
        <activity android:name=".DeliveryManProfile" />
        <activity android:name=".OrderDetails" />
        <activity android:name=".ViewProduct" />
        <activity android:name=".ViewCompany" />
        <activity android:name=".Empty" />
        <activity android:name=".ViewShopKepper" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".ProfileForCompany" />
        <activity android:name=".SubmitShopKeeperInfo" />
        <activity android:name=".SubmitCompanyInfo" />
        <activity android:name=".SubmitProduct" />
        <activity android:name=".ProfileForShopkeeper" />
        <activity android:name=".Login" />
        <activity android:name=".Registration" />
        <activity android:name=".MainActivity" />
        <activity android:name=".WelcomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>